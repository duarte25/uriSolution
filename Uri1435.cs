using  System;
class Uri
{
    static void Main(string[] args)
    {
        int n=Console.ReadLine();
        
        while (n != 0)
        {
            int[,] matriz = new int[n, n];
            int x;

            for (int i = 1; i <= n; i++) {
    			for (int j = 1; j <= n; j++) {
    				x = i;
                }
            }
        }

    }
}